
<section>
    <div>
      <ul >
        {% for block in section.blocks %}
          <li>
            <a href="{{ block.settings.link }}">
              {{ block.settings.link_text }}
            </a>
          </li>
        {% endfor %}
      </ul>
    </div>
  </section>
  {% schema %}
  {
    "name": "menu_h",
    "tag": "section",
    "class": "section",
    "settings": [],
    "presets": [
      {
        "name": "menu_h"
      }
    ],
    "blocks": [
      {
        "type": "menu_item",
        "name": "Menu Item",
        "settings": [
          {
            "type": "url",
            "id": "link",
            "label": "Link"
          },
          {
            "type": "text",
            "id": "link_text",
            "label": "Link Text"
          }
        ]
      }
    ]
  }
  {% endschema %}
  